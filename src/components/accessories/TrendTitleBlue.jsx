export const TrendTitleBlue = ({ title }) => {
  return (
    <main className="flex rounded-lg justify-center items-center py-1 px-2 bg-[var(--bg-blue)] text-[var(--default)]">
      {title}
    </main>
  );
};
